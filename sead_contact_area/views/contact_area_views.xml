<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_contact_area_tree" model="ir.ui.view">
        <field name="name">contact.area.tree</field>
        <field name="model">contact.area</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <record id="view_contact_area_form" model="ir.ui.view">
        <field name="name">contact.area.form</field>
        <field name="model">contact.area</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_contact_area_search" model="ir.ui.view">
        <field name="name">contact.area.search</field>
        <field name="model">contact.area</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
            </search>
        </field>
    </record>

    <record id="action_contact_area" model="ir.actions.act_window">
        <field name="name">Áreas</field>
        <field name="res_model">contact.area</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p>Define las áreas que pueden asignarse a los contactos.</p>
        </field>
    </record>
</odoo>
